/* eslint-env mocha */
/* eslint no-unused-expressions: 0 */

import Nav from './Nav';
import { expect } from 'chai';

describe('Nav', () => {
  it('works', () => {
    expect(Nav).not.to.be.null;
    // TODO
  });
});
