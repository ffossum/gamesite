import React from 'react';
import styles from '../userSettings.css';

export default class LocaleSettings extends React.Component {
  render() {
    return (
      <section className={styles.section}>
      </section>
    );
  }
}

LocaleSettings.propTypes = {
};
